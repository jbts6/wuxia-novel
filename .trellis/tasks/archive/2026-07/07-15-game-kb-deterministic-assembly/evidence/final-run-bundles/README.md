# Final v2 run bundles

这两个压缩包是在删除展开的 generate-game-kb 中间产物前生成的完整最终 run 归档。SHA-256、条目数、原 artifact manifest hash 见 manifest.json。

恢复时，在目标书籍的 _archive/generate-game-kb/ 下解压对应压缩包；压缩包顶层目录就是原 run-id。安装后的九类 data/*.json 与 ch_split/ 不依赖这些压缩包。
